// import { useLoaderData } from "react-router-dom";

const Poses = () => {
  // const posesData = useLoaderData;
  return (
    <div>
      Poses
      {/* {posesData} */}
    </div>
  );
};

export default Poses;
